# ============================================
# 生产环境 (prod)
# 部署命令: java -jar mytools.jar --spring.profiles.active=prod
# ============================================
spring:
  datasource:
    url: jdbc:mariadb://${生产地址ip}:3306/mytools?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    driver-class-name: org.mariadb.jdbc.Driver
    username: ${用户名}
    password: ${密码}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 3
      connection-timeout: 15000
      idle-timeout: 600000
      max-lifetime: 1800000

# 生产环境关闭 SQL 日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
